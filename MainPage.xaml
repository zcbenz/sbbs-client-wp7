<phone:PhoneApplicationPage 
    x:Class="sbbs_client_wp7.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:local="clr-namespace:sbbs_client_wp7"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="800" 
    d:DataContext="{d:DesignData SampleData/MainViewModelSampleData.xaml}"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="False">
    
    <phone:PhoneApplicationPage.Resources>
        <DataTemplate x:Key="MyItemHeaderTemplate">
            <Grid>
                <ContentPresenter>
                    <TextBlock Text="{Binding}" Foreground="{StaticResource PhoneAccentBrush}" FontSize="60" />
                </ContentPresenter>
            </Grid>
        </DataTemplate>
    </phone:PhoneApplicationPage.Resources>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
 
        <!--Panorama control-->
        <controls:Panorama Title="虎踞龙蟠BBS">
            <controls:Panorama.TitleTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontSize="90" Margin="20,60,0,0" />
                </DataTemplate>
            </controls:Panorama.TitleTemplate>
            
            <!--
            <controls:Panorama.Background>
                <ImageBrush ImageSource="PanoramaBackground.png"/>
            </controls:Panorama.Background>
            -->
 
            <!-- 我的最爱 -->
            <controls:PanoramaItem Header="我的最爱" HeaderTemplate="{StaticResource MyItemHeaderTemplate}">
                
            </controls:PanoramaItem>
            
            <!-- 我的账户 -->
            <controls:PanoramaItem Header="我的账户" HeaderTemplate="{StaticResource MyItemHeaderTemplate}">
                <toolkit:WrapPanel>
                    <toolkit:WrapPanel.Resources>
                        <Style TargetType="local:Tile">
                            <Setter Property="Margin" Value="5"/>
                        </Style>
                    </toolkit:WrapPanel.Resources>
                    
                    <local:Tile Title="热门版面"/>
                    <local:Tile Title="我的信箱"/>
                    <local:Tile Title="最近浏览"/>
                    <local:Tile Title="设置"/>
                    <local:Tile Title="退出"/>
                </toolkit:WrapPanel>
            </controls:PanoramaItem>

            <!-- 十大热帖 -->
            <controls:PanoramaItem Header="十大热帖" HeaderTemplate="{StaticResource MyItemHeaderTemplate}">
                <ScrollViewer>
                    <ListBox ItemsSource="{Binding Topten}" ItemTemplate="{StaticResource TopicDataTemplate}">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </ScrollViewer>
            </controls:PanoramaItem>

            <!-- 进站图片 -->
            <controls:PanoramaItem Header="进站图片" HeaderTemplate="{StaticResource MyItemHeaderTemplate}">

            </controls:PanoramaItem>

        </controls:Panorama>
    </Grid>
</phone:PhoneApplicationPage>